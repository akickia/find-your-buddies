import Button from "../Button";

const Choice = () => {
    return (
        <div>
            <Button name="Take left" link="#" />
            <Button name="Take right" link="#" />
        </div>
    )
};

export default Choice;